import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // experimental: {
  // Esta opción permite que dispositivos externos (celulares, otras PC)
  // se conecten a tu servidor de desarrollo sin que Next.js bloquee la petición.
  // @ts-ignore
  // allowedDevOrigins: [
  // "http://localhost:3000",
  // "http://*:3000"
  // ],
  // },
};

export default nextConfig;
